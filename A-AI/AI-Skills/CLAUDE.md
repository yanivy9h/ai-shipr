# AI-Skills — Index

51 single-task skills. Find the group that matches the work, then read only that skill's file. Six are typeable slash commands (marked); the rest are invoked by name or auto-invoked.

## Daily rhythms (typeable)

| Skill | Command | Does |
|---|---|---|
| `Today.md` | `/today` | Morning priority stack |
| `Weekly.md` | `/weekly` | Friday loop close |
| `Handoff.md` | `/handoff` | EOD summary or ownership transfer |
| `Review-Initiative.md` | `/review-initiative` | Weekly initiative health check |
| `Review-Experiment.md` | `/review-experiment` | Weekly experiment review |
| `Sync-Context.md` | `/sync-context` | Refresh `vp/shared/` mirror (team setups only) |

## Discovery & framing

`Working-Backwards` (PR/FAQ first, routes prototype-first or write-first) · `Prototype-Builder` (one uncertainty → runnable prototype; never demand questions) · `Competitive-Landscape-Mapper` (before user research) · `Interview-Guide-Builder` → `Interview-Synthesizer` → `Research-Synthesizer` (interview loop) · `Persona-Builder` · `JTBD-Mapper` · `Opportunity-Tree` · `Assumption-Extractor` → `Hypothesis-Builder` (assumption → falsifiable hypothesis) · `Edge-Case-Finder` · `Experiment-Designer` · `Discovery-Readiness-Checker` (gate before Definition / PRD-Builder)

## Prioritization & decisions

`Priority-Stack` (score and rank initiatives) · `Tradeoff-Mapper` (compare options structurally) · `OKR-Partner` (metrics with bet linkage)

## Stakeholder & communication

`Narrative-Refiner` (adapt thinking per audience) · `Cross-Team-Mapper` (dependencies and owners) · `1on1-Prep` (decision-first agenda) · `Board-Update-Builder` (exec/board/investor update) · `Meeting-Synthesizer` (raw meeting → decisions, actions, signals)

## Build phase

`PRD-Builder` (validated initiative → PRD) · `Build-Companion` (in-sprint decisions) · `Build-Review` (shipped vs committed) · `Fire-Responder` (incident triage)

## Post-launch & learning

`Performance-Tracker` (validate hypothesis with data) · `Retro-Facilitator` · `Sprint-Summarizer` (internal or stakeholder mode)

## Pitch & monetization

`Business-Case-Builder` → `Pitch-Deck-Builder` · `Pricing-Packaging` (value-based pricing and tiers)

## Planning & cycle (auto-invoked, not typeable)

`Sprint` (sprint planning) · `Plan` (roadmap: bets → sequenced initiative stack) · `Launch` (pre-launch go/no-go) · `Iterate` (post-data loop re-entry) · `Figma` (load synced Figma context)

## Setup & system

`Create-PM-Voice` (writing samples → PM-Voice.md) · `close-session-protocol` (session-close sweep — fired by the root CLAUDE.md rule, never typed)

## Lead mode only (`team_mode: lead`)

`Resource-Allocation-Mapper` · `Portfolio-Priority-Stack` · `Org-Design-Framer` · `Team-Health-Check` · `PM-Performance-Review` · `Hiring-Brief-Builder`

## Key context

Skills chain — common sequences and the full discovery chain are in `README.md` here. Each skill file carries its own frontmatter description; when unsure between two skills, read both descriptions before choosing.
