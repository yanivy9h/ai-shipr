# Changelog

Every release of AI-SHIPR, newest first. For what AI-SHIPR *is*, see [README.md](README.md).

Upgrading is always the same shape: unzip over your existing folder, keep your own content files, run `bash setup.sh` again. Your data lives in the `S-Strategy/`, `H-Hypotheses/`, `I-Initiatives/`, `P-Proof/` and `R-Relationships/` folders and is never touched by an upgrade.

---

## v4 (20 August 2026)

**v4 changes how AI-SHIPR loads context.** Nothing was removed, every v3 capability still works, and the folder names are unchanged.

* **The root `CLAUDE.md` went from 485 lines to 73.** It used to carry a full catalogue of every agent, skill, workflow and playbook, and all of it loaded on every session whether you needed it or not. Context now loads in three tiers: the root file always, folder indexes when Claude navigates to that folder, content files only when something actually needs them. **Measured result: about 77% fewer tokens loaded before you type anything.** Nothing was deleted, everything moved.
* **12 nested `CLAUDE.md` navigation indexes.** One in each major folder, plus one for each of the four `A-AI/` subfolders. They are maps, not content. Claude reads the index and goes straight to the right file instead of searching the repo, which is where the token saving comes from.
* **New `Session-Start.md`.** The four first-run and empty-state cases moved out of the root file into their own file, loaded only when one of them actually applies. A returning user with a populated system never pays for setup instructions again.
* **New `Connections.md`.** Six categories of tool to wire up: code, tickets, docs, analytics, user research, workspace. Each one lists the live connection, what it unlocks inside AI-SHIPR, a drop-zone fallback if you cannot connect it, and the question to take to your compliance team. Analytics is the one with the highest ceiling: unconnected, every metric conversation starts with copy-paste.
* **New measurement layer in `P-Proof/`.** `Metrics/` for definitions, `Queries/` for SQL, `Schemas/` for table docs, `Investigations/` for repeatable analysis methods, and `dashboards.md`. Queries carry a verification header, so a query is either blessed by whoever owns the data or explicitly marked unverified. Launching a feature now includes a gate for checking this in, and `PRD-Builder` writes a measurement plan as part of the PRD.
* **Planning discipline for major documents.** A new `When-Writing-a-Major-Doc` playbook, bringing the playbooks to 17. It covers picking a planning tier, an interview step where Claude challenges your assumptions before writing anything, saving plans into `I-Information/Templates/Plans/` so recurring work starts part-done, and pointing parallel subagents at temp files so a long document does not overflow the context window. `PRD-Builder`, `Board-Update-Builder`, `Pitch-Deck-Builder` and `Business-Case-Builder` each gained a Plan First step.
* **The restructure was tested, not assumed.** 20 realistic prompts run against the old and new structures. Both got 20 out of 20 to the correct capability, so cutting the always-loaded context cost nothing in routing accuracy. The honest tradeoff: the tiered structure sometimes takes more navigation turns to get there.

---

## v3.3 (5 August 2026)

**v3.3 fixes team setup.** If you are a solo PM, nothing here changes how you work.

* **`team_mode: individual` is reachable again.** `Settings.md` has always documented three team modes, but the Setup Worksheet only offered two, "Individual PM / Team lead", and Populate-Strategy mapped "Individual PM" to `solo`. A PM on a VP's team therefore ended up in `solo`, which means "no org layer to read", so their sessions silently skipped `../vp/shared/` forever. **Question 2 is now three-way: Solo PM / PM on a team / Team lead.**
* **Populate-Strategy generates the whole org layer.** Team leads now get all six `shared/` files. Previously `Vision.md`, `Strategy.md` and `OKRs.md` were required by the team layout but generated by nothing, so every team install hit a wall on day one. `shared/OKRs.md` is produced as an explicitly-labelled draft, since the worksheet collects metrics and bets rather than formal OKRs, with instructions to run `OKR-Partner` before the team reads it.
* **New Individual Mode step.** A PM on a team now reads the org layer *before* their files are generated, so their strategy comes out already aligned instead of needing reconciliation afterwards. It ladders each product bet to an org bet, flags conflicts without resolving them, and never writes to `shared/`.
* **New `/sync-context` command.** Run the org layer on Notion or Confluence, which have no file path, by keeping a local `vp/shared/` mirror and syncing it by page ID. Not needed on Google Drive or SharePoint, which mount directly. Slash commands go from 6 to 7.
* **Team setup, worksheet-first.** The Setup Worksheet now opens with the rule that matters most: **one worksheet per person, each answering about their own scope.** Full sequence at [verve-pm.com/ai-shipr-resources/team-rollout/](https://www.verve-pm.com/ai-shipr-resources/team-rollout/); where the files live, per tool, at [team-collab](https://www.verve-pm.com/ai-shipr-resources/team-collab/).
* **Corrections.** The AI-Skills README listed `/sprint`, `/iterate`, `/plan`, `/launch` and `/figma` as slash commands. All five are `user-invocable: false` and were never typeable. `Settings.md` pointed at a setup URL that no longer exists.

Also folded in, previously distributed only as zips: **`Working-Backwards`** (Amazon PR/FAQ method, routing to prototype-first or write-first) and **`Prototype-Builder`**.

---

## v3.2 (3 August 2026)

Distributed to workshop participants as a zip. Its contents were folded into the v3.3 notes above rather than documented separately.

---

## v3.1 (29 June 2026)

Shipped from three bug reports by a single workshop participant who took AI-SHIPR end to end through building a real product. Each report was traced to its structural cause rather than patched at the surface.

* **Three system-wide behaviour rules**, now in the root `CLAUDE.md`: no invented template fields, no ungrounded ordinal levels, no fabricated proper nouns. Together they close the category of failure where Claude quietly produces something plausible instead of flagging that it does not know.
* **`Stakeholders.md` gained a `## Rubrics` section** with behavioural anchors for Trust level, Decision role and Current sentiment, so ordinal fields have a definition to sit against.
* **Half-Sprint Day 2 rewritten** as "refine and finish", with an explicit note against skipping already-populated files. Day 2 had contradicted Day 1's auto-populate step.
* **`template-competitor-watch-list.md` added** to `I-Information/Templates/` as a defined home for competitive tracking.

Full detail is section 9 of [UPGRADE-to-v3.md](UPGRADE-to-v3.md).

---

## v3.0 (documented in this repo 16 July 2026)

**v3.0 changes how AI-SHIPR installs and how you invoke it.** Nothing was removed, every v2 capability still works.

* **Slash commands curated to 6.** Reserved for daily rhythms and the system guide: `/today`, `/weekly`, `/handoff`, `/review-initiative`, `/review-experiment`, `/shipr-guide`. The other ~70 capabilities are now invoked by name in conversation ("run Problem-Framer on this") or auto-invoked by Claude when the situation matches.
* **Everything installs as a Claude Code skill.** `setup.sh` scans agents, skills, and workflows, and installs each into `.claude/skills/`. About 73 capabilities total, driven by `name` / `description` / `user-invocable` frontmatter added across ~60 files. Run `bash setup.sh` once after cloning.
* **New capabilities.** `Pricing-Packaging` (value-based pricing and tiers), `Product-Signal-Reader` (turn raw analytics into signals), and a `close-session-protocol` that auto-captures decisions and learnings at session end.
* **Session hooks.** A memory-cue hook proposes learnings when you say "remember" or "from now on", and a staleness hook prompts a backfill when `Learning.md` has gone quiet for over a week.
* **Security boundaries.** Agents follow `_Security-Boundaries.md`, protecting internal strategy, people data, and stakeholder intelligence from extraction.

Upgrading from v2.x? See [UPGRADE-to-v3.md](UPGRADE-to-v3.md) for the full migration guide.

---

## v1.1 (9 March 2026)

Team Lead Mode, the Figma integration, the Duolingo worked example, and Linear as a ticket source.

---

## v1.0 (5 March 2026)

First release. The SHIPR folder structure (Strategy, Hypotheses, Initiatives, Proof, Relationships) with agents and skills on top.

---

## A note on version numbers

Between v3.0 and v3.3 the repo and the distributed zips drifted. The repo's README announced v3.0 while workshop participants were already holding zips labelled v3.1 and v3.2, so the repo jumped straight to v3.3 to land ahead of every artifact in circulation. If you have a zip numbered v3.1 or v3.2, it is newer than a repo checkout that says v3.0, not older.
